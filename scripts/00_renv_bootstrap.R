# After cloning:
#   Rscript -e 'install.packages("renv"); renv::restore()'
#
# This file is documentation only.

stop(
  "This script is documentation only. ",
  "Run renv::restore() interactively."
)
